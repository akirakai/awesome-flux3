[project]
name = "awesome-flux3-collector"
version = "0.1.0"
description = "Source-first hourly collector for FLUX.3 videos on X"
requires-python = ">=3.11"
license = { text = "MIT" }

[tool.ruff]
line-length = 110
target-version = "py311"
